/target/

# License private key - NEVER commit this
private_key.pem
*.pem

# User-uploaded POS images — stored on disk, not in version control
/uploads/

# Local npm deps for one-off menu image import script
/src/main/resources/db/import/node_modules/

# Claude Code machine-local settings
.claude/settings.local.json

# Cursor (no longer used)
.cursor/
